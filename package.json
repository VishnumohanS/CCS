{
  "name": "smart-health-encryption",
  "version": "1.0.0",
  "scripts": {
    "server": "npm run dev --prefix server",
    "client": "npm run dev --prefix client/app",
    "dev": "concurrently \"npm run server\" \"npm run client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
